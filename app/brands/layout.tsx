import type { ReactNode } from "react";

/** Enterprise marketing under /brands — no consumer AppShell chrome (ClientApp B2B prefix). */
export default function BrandsMarketingLayout({ children }: { children: ReactNode }) {
  return children;
}

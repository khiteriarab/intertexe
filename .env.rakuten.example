# Rakuten FTP + ingest (gitignored — never commit)

RAKUTEN_FTP_USER=rkp_4668007
RAKUTEN_FTP_USERNAME=rkp_4668007
# Set in your local shell or .env.production.local — do not commit real values.
RAKUTEN_FTP_PASSWORD=
RAKUTEN_FTP_HOST=aftp.linksynergy.com

# Required for live catalog writes from scripts / GHA
FEED_LIVE_INGEST_ENABLED=1

# Required for Vercel daily-catalog-refresh cron
EXPENSIVE_BACKGROUND_JOBS_ENABLED=1

# Optional: Supabase Management API token for MV refresh after sync
# SUPABASE_ACCESS_TOKEN=

"use client";

import { LifecycleHeroSection } from "./lifecycle/LifecycleHeroSection";

/** See it live — premium product-system lifecycle map + handoff into the walkthrough. */
export default function ProductLifecycleSystem() {
  return <LifecycleHeroSection />;
}

export function shareMessage(code: string): string {
  return `Have you tried Intertexe? It scans any clothing label and tells you exactly what your clothes are actually made of — no more greenwashing, no more synthetic surprises. Download it at intertexe.com and use my code ${code} to get started.`;
}
